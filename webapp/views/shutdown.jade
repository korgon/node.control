extends layout_minimal

block content
	.bigcirc(style='width: 420px; height: 420px;')
		.logincirc
			h1.bigblue(style='margin-top: 119px;') [shutdown]
			p.login(style='position: relative; top: -55px;') system powering down

block jscripts
	script(src="/socket.io/socket.io.js")
	script.
		// socket.io
		nodeio = io.connect();

		nodeio.emit('shutdown');
